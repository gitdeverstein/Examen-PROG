function getMax(t){
    let note_max;
    for (let i=0; i<t.length; i++){
        if(i==0){note_max=t[i];}
        if(t[i]>note_max){note_max=t[i];}
    }
    return note_max;
}
console.log(getMax([11,12,16,15,10,10.5]));

// Math.max(tab)===>affiche le plus grand
// Math.min(tab)===>affiche le plus petit

function getSecondMax(t,max){
    let secondMax;
    for(let i=0; i<t.length; i++){
        if(i==0 && t[i]>){
            note_max=t[0]
        }
    }
}